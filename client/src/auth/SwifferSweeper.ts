import SweepData from "./swiffer-data.json";
import SigAuth, { SigParameters } from "./SigAuth";

const SweepParameters = (low: SigParameters, high: SigParameters) =>
{
    const keys = Object.keys(low);
}

export default SweepParameters;